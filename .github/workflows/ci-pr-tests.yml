name: CI PR Tests

on:
  pull_request:
    branches:
      - main
      - dev

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install

      - name: Run map-tests
        run: pnpm run test:map

      - name: Run PerformanceTracker
        run: pnpm run test:performance

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: pr-report
          path: test-results/
          retention-days: 30

                - name: Post success comment to PR
        if: success()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            ✅ CI прошёл успешно

                  - name: Create bug issue on failure
        if: failure()
        uses: peter-evans/create-issue-from-file@v5
        with:
          title: "❌ CI Test Failed in ${{ github.workflow }}"
          content-filepath: .github/BUG_TEMPLATE.md
          labels: bug, ci-failure
          assignees: braslavskii1717

      - name: Send email alert on failure
        if: failure() && secrets.GMAIL_USER != ''
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.GMAIL_USER }}
          password: ${{ secrets.GMAIL_APP_PASS }}
          subject: "❌ CI failed: ${{ github.workflow }}"
          to: braslavskii1717@gmail.com
          from: GitHub Actions
          body: |
            CI workflow failed!
            
            Workflow: ${{ github.workflow }}
            Repository: ${{ github.repository }}
            Branch: ${{ github.ref }}
            Commit: ${{ github.sha }}
            Actor: ${{ github.actor }}
            
            View run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
